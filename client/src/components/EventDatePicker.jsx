import React from 'react';

const EventDatePicker = () => {
  return (
    <div className="text-gray-300 text-sm">
      EventDatePicker Component
      {/* TODO: Replace native input with a richer date picker (range limits, min/max constraints). */}
      {/* Consider disabling past dates and controlling max forecast horizon. */}
    </div>
  );
};

export default EventDatePicker;
