import React from 'react';

const LocationInput = () => {
  return (
    <div className="text-gray-300 text-sm">
      LocationInput Component
      {/* TODO: Implement UI for selecting / searching a location (city search or lat/lon inputs). */}
      {/* Could integrate a geocoding API or a map picker in future iterations. */}
    </div>
  );
};

export default LocationInput;
